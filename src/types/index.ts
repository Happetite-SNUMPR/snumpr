export interface NewsItem {
  id: string;
  title: string;
  details: string;
  imageUrl: string;
}

export interface HighlightItem {
  id: string;
  title: string;
  details: string;
  imageUrl: string;
}
